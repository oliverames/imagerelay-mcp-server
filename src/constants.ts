export const CHARACTER_LIMIT = 25000;
export const DEFAULT_PAGE_SIZE = 50;
export const USER_AGENT = "ImageRelayMCPServer/1.0 (mcp-server)";

export enum ResponseFormat {
  MARKDOWN = "markdown",
  JSON = "json",
}
